import React from 'react';
import { Box } from '@gluestack-ui/themed';

export default function HomeScreen() {
  return (
    // Un Box vacío que ocupa toda la pantalla
    <Box flex={1} bg="$backgroundLight100">
    </Box>
  );
}